controllers:
  main:
    containers:
      main:
        image:
          tag: v2.1.0
        env:
          REDIS_HOSTNAME: valkey.immich-valkey.svc.cluster.local
          IMMICH_MACHINE_LEARNING_URL: '{{ printf "http://%s-machine-learning:3003" .Release.Name }}'
          DB_URL:
            valueFrom:
              secretKeyRef:
                name: db-url
                key: DB_URL
          DB_VECTOR_EXTENSION: vectorchord
          DB_STORAGE_TYPE: SSD

immich:
  persistence:
    library:
      existingClaim: immich-media-pvc
